#include "Screen.h"
#include<iomanip>
#include "productManager.h"
#include "customer.h"

int main()
{
		/*int s = 2;
		float rateSeller = (int)5? s !=0 : 0;
		cout << rateSeller;*/
	//float n = 2.333333;
	//cout << setprecision(2);
	//cout << n;
	//fileManager::getProducts();
	//fileManager::getCustomers();
	//product p = allProducts[0] ;
	//customer  c(1,"j","er","eir","02394");
	//c.getCart()->fillCart(&p);
	//CartManager::rating(&c);
	//CartManager::setFinalRating(&c);
	//fileManager::clearAllProducts();
	//fileManager::saveProducts();


	//fileManager::getCustomers();
	//for (customer::itCustomer = customer::allcustomers.begin();customer::itCustomer != customer::allcustomers.end() ;customer::itCustomer++)
	//{
	//	cout << customer::itCustomer->second.getEmail() << endl;
	//}
	//
	/*cout << p.getratnum() << endl << p.getRate() << endl;
	allProducts[0].setrate(p.getRate());
	allProducts[0].setCountRate(p.getratnum());
	cout << allProducts[0].getratnum() << endl << allProducts[0].getRate();
	*/
	Screen::runApp();
	/*string name, email ;
	
	int id = 0;

		cin >> id >> name >> email;

		customer c(id, name, email);
		allcustomers.push_back(c);

		//Sorting::print_row(products[i].getId(), products[i].getName(), products[i].getPrice(), products[i].getQuantityOfuser(), (products[i].getRate() / products[i].getratnum()));

	
	fileManager::saveCustomers();
	fileManager::getCustomers();
	for (int i = 0; i < allcustomers.size(); i++)
	{
		cout << allcustomers[i].getid() << " " << allcustomers[i].getName() << " " << allcustomers[i].getEmail() << endl;

	}*/
	
	/*product p(1 , "phobe1" , 1 , "phone", 21, 123);
	product p1(2 , "phobe2" , 2 , "phone", 21, 123);
	product p2(3 , "phobe3" , 3 , "phone", 21, 123);
	product p3(4 , "phobe4" , 4 , "phone", 21, 123);
	product p4(5 , "phobe5" , 5 , "phone", 21, 123);
	product p5(6 , "phobe6" , 6 , "phone", 21, 123);
	customer c(1, "dfsdfsdg", " sd", "122", "324");
	c.getCart()->fillCart(&p);
	c.getCart()->fillCart(&p1);
	c.getCart()->fillCart(&p2);
	c.getCart()->fillCart(&p3);
	c.getCart()->fillCart(&p4);
	c.getCart()->fillCart(&p5);

	cout << c.getCart()->getTotalPrice();
	c.getCart()->printCartInfo();*/
	//Screen::runApp();
	//cout << setprecision(2);
	//float p = 3.2234;
	//cout << p << endl;
	//fileManager::getSellers();
	//fileManager::getCustomers();
	//fileManager::getProducts();

	////rateseller::sellerrate(&allseller[0]);
	////rateseller::sellerrate(&allseller[0]);
	////rateseller::sellerrate(&allseller[0]);
	////rateseller::sellerrate(&allseller[0]);
	////rateseller::sellerrate(&allseller[0]);

	//cout << allseller[0].getRate() << endl;

	//fileManager::clearAllFiles();
	//fileManager::saveCustomers();
	//fileManager::saveProducts();
	//fileManager::saveSellers();
	//fileManager::getProducts();
	/*for (size_t i = 0; i <product::allProducts.size(); i++)
	{
		cout << product::allProducts[i].getPrice() << endl;
	}*/
	//Sorting::print_data(product::allProducts);

	//Screen::runApp();

	}

		